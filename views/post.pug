extends layout

block content
  h1 #{post.title}
  h2 #{post.author}
  h3 #{post.body}
  br
  h4 #{post.date}
  hr
  a.btn.btn-default(href='/post/edit/'+post._id) Edit
  a.btn.btn-danger(href='/post/delete/'+post._id,) DELETE
